# Nixpacks configuration for Next.js on Railway
# Toto zabezpečí, že Railway správne detekuje Next.js aplikáciu

[phases.setup]
nixPkgs = ["nodejs-20_x", "nodePackages.npm"]

[phases.install]
cmds = ["npm install"]

[phases.build]
cmds = ["npm run build"]

[start]
cmd = "npm start"
