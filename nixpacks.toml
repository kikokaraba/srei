# Nixpacks configuration for Next.js on Railway
# Toto zabezpečí, že Railway správne detekuje Next.js aplikáciu

[phases.setup]
nixPkgs = ["nodejs-18_x", "nodePackages.npm"]

[phases.install]
cmds = ["npm ci"]

[phases.build]
cmds = ["npm run build"]

[start]
cmd = "npm start"
