{
  "$schema": "https://railway.app/railway.schema.json",
  "deploy": {
    "startCommand": "(npx prisma db push --accept-data-loss || true) && npm run db:auto-seed && (npx tsx scripts/start-cron.ts &) && node .next/standalone/server.js",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 10
  }
}
