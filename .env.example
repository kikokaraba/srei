# Database (Railway PostgreSQL)
# Railway poskytuje dve URL:
# 1. INTERNÁ URL (pre Railway services): postgres.railway.internal:5432
#    - Funguje LEN v Railway prostredí (production)
# 2. EXTERNÁ URL (pre lokálny vývoj): *.proxy.rlwy.net alebo *.railway.app
#    - Funguje z tvojho počítača
# 
# Pre lokálny vývoj:
# - V Railway dashboarde → PostgreSQL → "Connect" → "Public Network"
# - Skopíruj externú URL a pridaj ?sslmode=require
# 
# Príklad externej URL:
# DATABASE_URL="postgresql://postgres:PASSWORD@HOST.proxy.rlwy.net:PORT/railway?sslmode=require"
#
# Pre production (v Railway):
# Railway automaticky nastaví DATABASE_URL s internou URL
DATABASE_URL="postgresql://postgres:PASSWORD@HOST:PORT/railway?sslmode=require"

# NextAuth
NEXTAUTH_SECRET="your-secret-key-here"
NEXTAUTH_URL="http://localhost:3000"

# Upstash Redis (for rate limiting)
UPSTASH_REDIS_REST_URL="https://your-redis-instance.upstash.io"
UPSTASH_REDIS_REST_TOKEN="your-redis-token-here"

# Cron Jobs Security
CRON_SECRET="your-cron-secret-here"

# Notifications (optional)
# Webhook pre Slack/Discord notifikácie o nových NBS dátach
NOTIFICATION_WEBHOOK_URL="https://hooks.slack.com/services/xxx/yyy/zzz"

# Email notifikácie (optional)
NOTIFICATION_EMAIL="admin@example.com"
SMTP_HOST="smtp.example.com"
SMTP_PORT="587"
SMTP_USER="user@example.com"
SMTP_PASSWORD="password"

# Mapbox (for interactive map with clustering)
# Get your free token at: https://account.mapbox.com/access-tokens/
# Free tier: 50,000 map loads/month
NEXT_PUBLIC_MAPBOX_TOKEN="pk.your-mapbox-access-token-here"

# Scraper - Browser Configuration
# Option 1: Remote browser service (Browserless, Bright Data)
# Browserless.io: wss://chrome.browserless.io?token=YOUR_API_KEY
# Bright Data: wss://CUSTOMER_ID:PASSWORD@brd.superproxy.io:PORT
BROWSER_WS_ENDPOINT=""

# Option 2: Local browser executable path (pre Railway/Docker)
# BROWSER_EXECUTABLE_PATH="/usr/bin/google-chrome"

# Engine API Key (pre externé triggery syncu)
ENGINE_API_KEY="your-engine-api-key-here"
